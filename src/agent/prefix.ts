/**
 * User-visible Agent Contract line prefix (verbatim display).
 * Hosts must copy this exactly — do not paraphrase to "JEV:" or other short forms.
 */
export const AGENT_CONTRACT_PREFIX = "JEVCore:";
